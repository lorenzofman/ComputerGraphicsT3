#pragma once

#include "Color.h"
#include "Types.h"
namespace Colors
{
	static const Color Background = Color(0.15f, 0.15f, 0.15f);
}

constexpr float PI = 3.14159265359f;
constexpr float DegToRad = PI / 180.0f;